#include <iostream>
#include <cstdio>      // Including c standard input/output 
using namespace std;

int main() {
    // Complete the code.
    int a;
    long b;
    char c;
    float d;
    double e;
    scanf("%d %ld %c %f %lf",&a ,&b , &c, &d, &e);      //This is a c input function. This can be done using c++ input function cin, but the code will be long
    printf("%d\n%ld\n%c \n%f \n%lf",a ,b , c, d, e);    //This is a c output function. This can be done using c++ output function cout, but the code will be long

    return 0;
}
